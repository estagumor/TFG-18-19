import { StartDateLimitationDirective } from './start-date-limitation.directive';

describe('StartDateLimitationDirective', () => {
  it('should create an instance', () => {
    const directive = new StartDateLimitationDirective();
    expect(directive).toBeTruthy();
  });
});

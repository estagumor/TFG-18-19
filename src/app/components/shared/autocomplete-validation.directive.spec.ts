import { AutocompleteValidationDirective } from './autocomplete-validation.directive';

describe('AutocompleteValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteValidationDirective();
    expect(directive).toBeTruthy();
  });
});

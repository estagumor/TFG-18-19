import { LeadersRestrictionDirective } from './leaders-restriction.directive';

describe('LeadersRestrictionDirective', () => {
  it('should create an instance', () => {
    const directive = new LeadersRestrictionDirective();
    expect(directive).toBeTruthy();
  });
});
